let btnSubmit = document.getElementById('buttonsubmit');

btnSubmit.addEventListener('click', (e) =>{
    e.preventDefault();
    let kname = document.getElementById('kname').value;
    let kpass = document.getElementById('kpass').value;
    console.log(kname);
    console.log(kpass);
    if(!kname.Contains('@')){
        console.log("thêm ký tự @ đi");
        alert('tài khoản méo tồn tại')
    }
    else{
    // document.getElementById("form").submit();
    if(password === "123"){
        DOM.
    }
}
});

// lùi lại để tìm file
window.location.href = "./hihi/home.html";